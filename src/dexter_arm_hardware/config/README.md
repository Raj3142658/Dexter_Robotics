# dexter_arm_hardware config

This directory is required by `dexter_arm_hardware/CMakeLists.txt` install rules.

Current workspace was bootstrapped with a reduced package set for middleware/UI prototyping,
so hardware MoveIt config files are intentionally not populated yet.
